module.exports = {
  reactStrictMode: true,
  exportTrailingSlash: true, // This adds a trailing slash to the URL paths
  basePath: '/your-repository-name',  // Optional, only if needed for GitHub Pages
  assetPrefix: '/your-repository-name/', // Optional, only if needed for GitHub Pages
};